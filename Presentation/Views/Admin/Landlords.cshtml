@model Presentation.Models.AdminLandlordViewModel

@{
    ViewData["Title"] = "Manage Landlords";
}

<div class="container mt-4">
    <h1>Manage Landlords</h1>
    
    <div class="mb-3">
        <a asp-action="Dashboard" class="btn btn-secondary">Back to Dashboard</a>
    </div>

    <form asp-action="Landlords" method="get" class="mb-3">
        <div class="row">
            <div class="col-md-4">
                <input type="text" name="search" class="form-control" placeholder="Search landlords..." value="@Model.Search" />
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary">Search</button>
            </div>
            <div class="col-md-2">
                <a asp-action="Landlords" class="btn btn-outline-secondary">Clear</a>
            </div>
        </div>
    </form>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Email</th>
                <th>Company Name</th>
                <th>National ID</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Landlords)
            {
                <tr>
                    <td>@(item.User?.Email)</td>
                    <td>@Html.DisplayFor(modelItem => item.CompanyName)</td>
                    <td>@Html.DisplayFor(modelItem => item.NationalId)</td>
                    <td>
                        @if (item.User?.Status == Shared.Enums.UserStatus.Pending)
                        {
                            <span class="badge bg-warning">Pending</span>
                        }
                        else if (item.User?.Status == Shared.Enums.UserStatus.Approved)
                        {
                            <span class="badge bg-success">Approved</span>
                        }
                        else if (item.User?.Status == Shared.Enums.UserStatus.Rejected)
                        {
                            <span class="badge bg-danger">Rejected</span>
                        }
                    </td>
                    <td>
                        <a asp-action="LandlordDetails" asp-route-id="@item.LandLordId" class="btn btn-sm btn-info">Details</a>
                        @if (item.User?.Status == Shared.Enums.UserStatus.Pending)
                        {
                            <a asp-action="ApproveLandlord" asp-route-id="@item.LandLordId" class="btn btn-sm btn-success">Approve</a>
                            <a asp-action="RejectLandlord" asp-route-id="@item.LandLordId" class="btn btn-sm btn-danger">Reject</a>
                        }
                        <a asp-action="DeleteLandlord" asp-route-id="@item.LandLordId" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <nav aria-label="Page navigation">
        <ul class="pagination">
            @if (Model.HasPreviousPage)
            {
                <li class="page-item">
                    <a class="page-link" asp-action="Landlords" asp-route-pageNumber="@(Model.CurrentPage - 1)" 
                       asp-route-pageSize="@Model.PageSize" asp-route-search="@Model.Search">Previous</a>
                </li>
            }

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a class="page-link" asp-action="Landlords" asp-route-pageNumber="@i" 
                       asp-route-pageSize="@Model.PageSize" asp-route-search="@Model.Search">@i</a>
                </li>
            }

            @if (Model.HasNextPage)
            {
                <li class="page-item">
                    <a class="page-link" asp-action="Landlords" asp-route-pageNumber="@(Model.CurrentPage + 1)" 
                       asp-route-pageSize="@Model.PageSize" asp-route-search="@Model.Search">Next</a>
                </li>
            }
        </ul>
    </nav>

    <div class="mt-3">
        Showing @Model.Landlords.Count() of @Model.TotalCount landlords
    </div>
</div>
